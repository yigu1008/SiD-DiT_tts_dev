description: fksteering-actdiff-sid-ir

# "Drop ActDiff action axes into existing search methods" grid experiment.
#
# Two compute tiers per existing method:
#   _cfg   = budget-friendly: vary CFG bank only
#   _full  = compute-heavy:   vary CFG bank x 3-level prompt rewrites
#
# Methods (11), one suite invocation:
#   baseline
#   bon                (canonical, fixed cfg/variant)         <- reference
#   bon_actdiff_cfg    (cfg axis only)                        <- budget BoN+ActDiff
#   bon_actdiff_full   (cfg x prompt)                         <- full BoN+ActDiff
#   sop                (canonical, fixed cfg/variant)         <- reference
#   sop_actdiff_cfg    (cfg axis at branching)                <- budget SoP+ActDiff
#   sop_actdiff_full   (cfg x prompt at branching)            <- full SoP+ActDiff
#   smc                (canonical particles, fixed cfg)       <- reference
#   smc_actdiff_cfg    (cfg axis at resample)                 <- budget SMC+ActDiff
#   smc_actdiff_full   (cfg x prompt at resample)             <- full SMC+ActDiff
#   bon_mcts           (full ActDiff = prescreen + MCTS refine)<- upper-bound ref
#
# Cross-product runs:
#   backend  in { sid, senseflow_large, sd35_base, flux_schnell-via-other-suite }
#   reward   in { composite_hpsv3_ir, composite_all4 }
#
# Each (backend, reward) combination is a separate submit -- override via
# `-p backend=... -p search_reward=...`.  Defaults below are sid x composite_hpsv3_ir.
#
# Output: /mnt/data/v-yigu/all_method_ablations/{run_tag}/run_*/
#   {baseline, bon, bon_actdiff_*, sop, sop_actdiff_*, bon_mcts}/...
#   summary.tsv

target:
  service: sing
  name: GenAI-Shared-UKSouth2
  workspace_name: Singularity-GenAI-WS-UKSouth2
  resource_group: singularity-genai-gpu-uksouth2

env_defaults:
  service: sing
  PRIORITY: high
  NODES: 1
  GPUS: 8
  MEM: 80

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04
  setup:
    - export PATH=/home/aiscuser/.local/bin:$$PATH
    - pip install --user datasets huggingface_hub hpsv2

code:
  local_dir: ./

storage:
  output:
    storage_account_name: vigstandard
    container_name: data
    mount_dir: /mnt/data

search:
  job_template:
    name: allmeth-{run_tag}
    sku: ${NODES}x${MEM}G${GPUS}
    sla_tier: Standard
    process_count_per_node: 1
    priority: high
    mpi: false
    command:
      - set -x
      - export PYTHONNOUSERSITE=1
      - unset PYTHONPATH || true
      - export SID_FORCE_WANDB_STUB=1
      - export WANDB_DISABLED=true
      - export PATH=/home/aiscuser/.local/bin:$$PATH
      - export PATH="$$PATH:/home/aiscuser/.local/bin:/root/.local/bin:/opt/conda/envs/ptca/bin"
      - export PYTHON_BIN=/opt/conda/envs/ptca/bin/python
      - export HF_HOME=/mnt/data/v-yigu/model_cache/hf_cache
      - export HF_TOKEN={hf_token}
      - export HUGGING_FACE_HUB_TOKEN={hf_token}
      - export NCCL_ASYNC_ERROR_HANDLING=0
      - export NCCL_P2P_LEVEL=NVL
      # ?? Sampling env ----
      - source /opt/conda/etc/profile.d/conda.sh
      - /opt/conda/envs/ptca/bin/pip install --upgrade "setuptools>=70,<76" wheel
      - /opt/conda/envs/ptca/bin/pip install -r requirements.txt
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir --force-reinstall --no-deps "xformers==0.0.31.post1" || true
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir --no-deps "xxhash>=3.4.1" "ftfy>=6.2.3" "regex>=2024.11.6" "tqdm>=4.66.4"
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "datasets>=2.19.0"
      # ?? Cache + preload weights ----
      - export NUM_GPUS=${GPUS}
      - export IMAGEREWARD_CACHE=/mnt/data/v-yigu/model_cache/ImageReward
      - export HPS_ROOT=/mnt/data/v-yigu/model_cache/hpsv2
      - mkdir -p /mnt/data/v-yigu/model_cache/hf_cache /mnt/data/v-yigu/model_cache/ImageReward /mnt/data/v-yigu/model_cache/hpsv2
      - mkdir -p /home/aiscuser/.cache
      - ln -sfn /mnt/data/v-yigu/model_cache/ImageReward /home/aiscuser/.cache/ImageReward || true
      - export ALL_REPOS='YGu1998/SiD-DiT-SD3.5-large stabilityai/stable-diffusion-3.5-large domiso/SenseFlow yuvalkirstain/PickScore_v1'
      - for repo in $$ALL_REPOS; do /opt/conda/envs/ptca/bin/python -c "from huggingface_hub import snapshot_download; import os, sys; snapshot_download(sys.argv[1], token=os.environ.get('HF_TOKEN'), resume_download=True, max_workers=1); print('cache OK:', sys.argv[1])" "$$repo" || ( echo "FATAL prefetch failed for $$repo"; exit 1 ); done
      - for repo in $$ALL_REPOS; do dir="$$HF_HOME/hub/models--$$(echo $$repo | sed 's|/|--|g')"; if ! ls $$dir/snapshots/*/* >/dev/null 2>&1; then echo "FATAL cache verify failed for $$repo (looked in $$dir)"; exit 1; fi; echo "verify_OK $$repo"; done
      # ?? Reward env + ImageReward fix-up ----
      - export REWARD_ENV_NAME=reward
      - export REWARD_ENV_SENTINEL_DIR=/tmp
      - CONDA_BASE=/opt/conda REWARD_ENV_NAME="$$REWARD_ENV_NAME" REWARD_ENV_SENTINEL_DIR="$$REWARD_ENV_SENTINEL_DIR" bash setup_reward_env.sh
      - export REWARD_ENV_PREFIX=$$(cat "$$REWARD_ENV_SENTINEL_DIR/reward_env_prefix_$$REWARD_ENV_NAME")
      - export REWARD_PY=$$REWARD_ENV_PREFIX/bin/python
      - $$REWARD_PY -m pip install --no-cache-dir --upgrade "setuptools<80" "wheel" || true
      - $$REWARD_PY -m pip install --no-cache-dir "fairscale==0.4.4" || true
      - $$REWARD_PY -m pip install --no-cache-dir --no-deps "diffusers>=0.27,<0.31" || true
      - $$REWARD_PY -m pip install --no-cache-dir --no-deps "datasets" "wandb" || true
      - 'IR_INIT=$$($$REWARD_PY -c "import importlib.util; s=importlib.util.find_spec(\"ImageReward\"); print(s.origin if s else \"\")" 2>/dev/null || echo ""); if [ -n "$$IR_INIT" ] && [ -f "$$IR_INIT" ]; then grep -q "^# (patched-no-ReFL)" "$$IR_INIT" || sed -i "s|^from \\.ReFL import|# (patched-no-ReFL) from .ReFL import|" "$$IR_INIT"; fi'
      - $$REWARD_PY -c "import ImageReward; print('[verify] ImageReward', ImageReward.__file__)"
      # ?? Reward server (ImageReward = search reward; HPSv3 = eval-only) ----
      # Search optimizes ImageReward (see search_reward=imagereward); HPSv3 is
      # served too so the final eval can score it (the ptca sampling env cannot
      # import hpsv3 locally).
      - export REWARD_BACKENDS_SERVER='imagereward hpsv3'
      - export STAGE_REWARD_WEIGHTS_LOCAL=1
      - export REWARD_LOCAL_HF_CACHE=/tmp/hf_cache_local
      - REWARD_BACKENDS="$$REWARD_BACKENDS_SERVER" $$REWARD_PY preload_reward_models.py
      - 'if [ -f "$$REWARD_LOCAL_HF_CACHE/.stage_done" ]; then export REWARD_HF_HOME="$$REWARD_LOCAL_HF_CACHE"; else export REWARD_HF_HOME="$$HF_HOME"; fi'
      - export PARENT_ROOT=/mnt/data/v-yigu/all_method_ablations/{run_tag}
      - export RUN_ROOT=$$PARENT_ROOT
      - mkdir -p "$$RUN_ROOT"
      - export REWARD_SERVER_PORT={reward_server_port}
      - export REWARD_SERVER_URL=http://localhost:$$REWARD_SERVER_PORT
      - export REWARD_SERVER_LOG=$$RUN_ROOT/reward_server.log
      - env -u NCCL_P2P_LEVEL -u NCCL_ASYNC_ERROR_HANDLING -u TORCH_NCCL_ASYNC_ERROR_HANDLING CUDA_VISIBLE_DEVICES=0 TOKENIZERS_PARALLELISM=false HF_HOME="$$REWARD_HF_HOME" "$$REWARD_PY" -u reward_server.py --port "$$REWARD_SERVER_PORT" --device cuda:0 --backends $$REWARD_BACKENDS_SERVER --image_reward_model ImageReward-v1.0 > "$$REWARD_SERVER_LOG" 2>&1 &
      - export REWARD_SERVER_PID=$$!
      - trap 'kill $$REWARD_SERVER_PID >/dev/null 2>&1 || true' EXIT
      - export HEALTH_OK=0
      - for i in $$(seq 1 1200); do if curl -s "$$REWARD_SERVER_URL/health" >/dev/null 2>&1; then export HEALTH_OK=1; break; fi; if ! kill -0 "$$REWARD_SERVER_PID" >/dev/null 2>&1; then echo "Reward server died"; tail -n 200 "$$REWARD_SERVER_LOG"; exit 1; fi; sleep 3; done
      - if [ "$$HEALTH_OK" != "1" ]; then echo "Reward server failed to become healthy"; tail -n 400 "$$REWARD_SERVER_LOG"; exit 1; fi
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "transformers==4.52.4" "tokenizers==0.21.1" "qwen-vl-utils==0.0.14"
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "pandas>=2.1.4" "pyarrow>=14.0.2" "datasets>=2.19.0"
      # ?? Sampling restricted to GPUs 1..N-1 (server owns GPU 0) ----
      - export SAMPLING_GPUS=$$((${GPUS} - 1))
      - export CUDA_VISIBLE_DEVICES_SAMPLE=$$(seq -s, 1 $$SAMPLING_GPUS)
      - export NUM_GPUS=$$SAMPLING_GPUS
      - export HF_HUB_OFFLINE=1
      - export TRANSFORMERS_OFFLINE=1
      # ?? Bank overrides ----
      - export BON_MCTS_NEG_BANK_NEG="{neg_bank}"
      - export BON_MCTS_SIGMA_BANK_SIGMA="{sigma_bank}"
      - export BON_MCTS_NEG_BANK_AXES="{neg_bank}"
      - export BON_MCTS_SIGMA_BANK_AXES="{sigma_bank}"
      # ?? Multi-model loop: 3 SD-family backends in one job ----
      # One persistent reward server (started above) serves all backends; each
      # backend writes to $PARENT_ROOT/<backend>. Pays env build + weight
      # prefetch + server warmup ONCE instead of per-model. Sequential, so
      # wall-clock ~3x but only 1 node held.
      - export N_PROMPTS={n_prompts}
      - export SEED={seed}
      - export SEARCH_REWARD={search_reward}
      # Final eval rewards: add PickScore alongside IR/hpsv3 (search reward stays
      # ImageReward-only on the server). PickScore_v1 prefetched above so the
      # offline eval (ptca python, shared HF_HOME) can load it.
      - export EVAL_BACKENDS="imagereward hpsv3 pickscore"
      - export PICKSCORE_MODEL=yuvalkirstain/PickScore_v1
      - export METHODS="{methods}"
      - export PYTHON_BIN=/opt/conda/envs/ptca/bin/python
      # Compute-matched BoN: NFE = (N_SEEDS + N_SIMS) x STEPS
      # sid/senseflow 4-step: 16 + 60 = 76; sd35_base 28-step: 16 + 120 = 136.
      - export BACKEND_LIST="{backend_list}"
      - for b in $$BACKEND_LIST; do echo "[multimodel] ===== backend $$b ====="; export BACKEND=$$b; export RUN_ROOT=$$PARENT_ROOT/$$b; mkdir -p "$$RUN_ROOT"; if [ "$$b" = "sd35_base" ]; then export BON_N={bon_n_long}; else export BON_N={bon_n_short}; fi; bash run_all_method_ablations.sh; done
      - export RUN_ROOT=$$PARENT_ROOT
      # ?? Auto-emit synergy plot ----
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir matplotlib || true
      # (synergy plot omitted -- actdiff_grid does not include 2x2 corner cells)

    submit_args:
      env:
        DATASET_MOUNT_BLOCK_BASED_CACHE_ENABLED: "true"
        SHARED_MEMORY_PERCENT: "0.5"

  max_trials: 1
  type: grid
  params:
    - name: run_tag
      values:
        - "fksteering-actdiff-sid-ir"
    - name: hf_token
      values:
        - "YOUR_HF_TOKEN_HERE"
    - name: backend_list
      values:
        - "sid senseflow_large sd35_base" # 3 SD-family models, one job
    - name: n_prompts
      values:
        - 100
    - name: seed
      values:
        - 42
    - name: search_reward
      # ImageReward-only search (matches the "-ir" run name). HPSv3 is reported
      # at eval via the reward server. Override with -p search_reward=... if you
      # want the composite to drive MCTS instead.
      values:
        - "imagereward"
    - name: methods
      values:
        - "fksteering_actdiff_cfg fksteering_actdiff_full"
    # BON_N defaults to matched-NFE (76 for 4-step, 136 for sd35_base).
    # bon_actdiff_* uses the same N but spreads across the action bank.
    - name: neg_bank
      values:
        - "||low quality, blurry, lowres, jpeg artifacts||bad anatomy, deformed, mutated, extra limbs||watermark, signature, text, frame, cropped"
    - name: sigma_bank
      values:
        - "-0.05 0.0 0.05"
    - name: bon_n_short
      values:
        - 76 # matched NFE for 4-step backends (16 + 60 = 76)
    - name: bon_n_long
      values:
        - 136 # matched NFE for sd35_base 28-step (16 + 120 = 136)
    - name: reward_server_port
      values:
        - 5125
