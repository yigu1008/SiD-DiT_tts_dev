description: flux-schnell-dts-hpsv3-server

# DTS / DTS* on FLUX-schnell with HPSv3 as the search-time reward.
#
# Migrated from the old in-process reward pattern (which can't host HPSv3
# alongside HPSv2 because they need different transformers versions) to the
# isolated reward-server pattern: HPSv3 + ImageReward run on GPU 0 inside the
# `reward` conda env (transformers==4.45.2). Sampling runs on GPUs 1..N-1.
# HPSv2 + PickScore are evaluated post-hoc via separate per-backend servers
# (hosting all four at once OOMs the single GPU 0).
#
# Phase-1 eval covers BOTH search-time evaluators (hpsv3 + imagereward).
# Phase-2 fills hpsv2 + pickscore.

target:
  service: sing
  name: GenAI-Shared-UKSouth2
  workspace_name: Singularity-GenAI-WS-UKSouth2
  resource_group: singularity-genai-gpu-uksouth2

env_defaults:
  service: sing
  PRIORITY: high
  NODES: 1
  GPUS: 8
  MEM: 80

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04
  setup:
    - export PATH=/home/aiscuser/.local/bin:$$PATH
    - pip install --user datasets huggingface_hub hpsv2

code:
  local_dir: ./

storage:
  output:
    storage_account_name: vigstandard
    container_name: data
    mount_dir: /mnt/data

search:
  job_template:
    name: flux-schnell-dts-hpsv3-{run_tag}
    sku: ${NODES}x${MEM}G${GPUS}-IB
    sla_tier: Standard
    process_count_per_node: 1
    priority: high
    mpi: false
    command:
      - set -x
      - unset REWARD_BACKEND REWARD_TYPE REWARD_BACKENDS || true
      - export PYTHONNOUSERSITE=1
      - unset PYTHONPATH || true
      - export SID_FORCE_WANDB_STUB=1
      - export WANDB_DISABLED=true
      # ── Paths & HF auth ────────────────────────────────────────────────────
      - export PATH=/home/aiscuser/.local/bin:$$PATH
      - export PATH="$$PATH:/home/aiscuser/.local/bin:/root/.local/bin:/opt/conda/envs/ptca/bin"
      - export PYTHON_BIN=/opt/conda/envs/ptca/bin/python
      - export HF_HOME=/mnt/data/v-yigu/model_cache/hf_cache
      - export HF_TOKEN={hf_token}
      - export HUGGING_FACE_HUB_TOKEN={hf_token}
      - export FLUX_BACKEND=flux
      - export AZFUSE_USE_FUSE=0
      - export NCCL_ASYNC_ERROR_HANDLING=0
      - export NCCL_P2P_LEVEL=NVL
      # ── Install deps (sampling env) ────────────────────────────────────────
      - source /opt/conda/etc/profile.d/conda.sh
      - /opt/conda/envs/ptca/bin/pip install --upgrade "setuptools>=70,<76" wheel
      - /opt/conda/envs/ptca/bin/pip install -r requirements.txt
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir --force-reinstall --no-deps "xformers==0.0.31.post1" || true
      - /opt/conda/envs/ptca/bin/python -c "import xformers, xformers.ops; print('xformers ok', xformers.__version__)" || (/opt/conda/envs/ptca/bin/pip uninstall -y xformers && echo 'xformers disabled')
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir --no-deps "xxhash>=3.4.1" "ftfy>=6.2.3" "regex>=2024.11.6" "tqdm>=4.66.4"
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "pandas>=2.1.4" "pyarrow>=14.0.2" "datasets>=2.19.0"
      - unset HF_HUB_OFFLINE TRANSFORMERS_OFFLINE || true
      # ── Cache dirs ─────────────────────────────────────────────────────────
      - export NUM_GPUS=${GPUS}
      - export IMAGEREWARD_CACHE=/mnt/data/v-yigu/model_cache/ImageReward
      - export HPS_ROOT=/mnt/data/v-yigu/model_cache/hpsv2
      - export CLIP_CACHE_DIR=/mnt/data/v-yigu/model_cache/clip
      - mkdir -p /mnt/data/v-yigu/model_cache/hf_cache /mnt/data/v-yigu/model_cache/ImageReward /mnt/data/v-yigu/model_cache/hpsv2 /mnt/data/v-yigu/model_cache/clip
      - mkdir -p /home/aiscuser/.cache
      - ln -sfn /mnt/data/v-yigu/model_cache/clip /home/aiscuser/.cache/clip || true
      - ln -sfn /mnt/data/v-yigu/model_cache/ImageReward /home/aiscuser/.cache/ImageReward || true
      # ── Preload FLUX-schnell weights ───────────────────────────────────────
      - export HF_HUB_ENABLE_HF_TRANSFER=0
      - /opt/conda/envs/ptca/bin/python -c "from huggingface_hub import snapshot_download; import os; snapshot_download('black-forest-labs/FLUX.1-schnell', cache_dir=os.environ.get('HF_HOME'), token=os.environ.get('HF_TOKEN'), resume_download=True, max_workers=1); print('FLUX.1-schnell cache OK')"
      # ── Build isolated reward env (HPSv3 + ImageReward) ────────────────────
      - export REWARD_ENV_NAME=reward
      - export REWARD_ENV_SENTINEL_DIR=/tmp
      - CONDA_BASE=/opt/conda REWARD_ENV_NAME="$$REWARD_ENV_NAME" REWARD_ENV_SENTINEL_DIR="$$REWARD_ENV_SENTINEL_DIR" bash setup_reward_env.sh
      - export REWARD_ENV_PREFIX=$$(cat "$$REWARD_ENV_SENTINEL_DIR/reward_env_prefix_$$REWARD_ENV_NAME")
      - export REWARD_PY=$$REWARD_ENV_PREFIX/bin/python
      - 'if [ ! -x "$$REWARD_PY" ]; then echo "Reward env python not found"; exit 1; fi'
      - $$REWARD_PY -c "import torch, transformers; print('reward env torch='+torch.__version__, 'transformers='+transformers.__version__)"
      # ── ImageReward fix-up cascade (setuptools/fairscale/diffusers/ReFL) ───
      - $$REWARD_PY -m pip install --no-cache-dir --upgrade "setuptools<80" "wheel" || true
      - $$REWARD_PY -m pip install --no-cache-dir "fairscale==0.4.4" || $$REWARD_PY -m pip install --no-cache-dir "fairscale" || true
      - $$REWARD_PY -m pip install --no-cache-dir --no-deps "diffusers>=0.27,<0.31" || $$REWARD_PY -m pip install --no-cache-dir --no-deps "diffusers" || true
      - $$REWARD_PY -m pip install --no-cache-dir --no-deps "datasets" "wandb" || true
      - $$REWARD_PY -c "import ImageReward; print('[verify] ImageReward', ImageReward.__file__)" || $$REWARD_PY -m pip install --no-cache-dir --no-deps --no-build-isolation "git+https://github.com/THUDM/ImageReward.git" || $$REWARD_PY -m pip install --no-cache-dir --no-deps "git+https://github.com/THUDM/ImageReward.git" || $$REWARD_PY -m pip install --no-cache-dir --no-deps "image-reward==1.5" || true
      - 'IR_INIT=$$($$REWARD_PY -c "import importlib.util; s=importlib.util.find_spec(\"ImageReward\"); print(s.origin if s else \"\")" 2>/dev/null || echo ""); if [ -n "$$IR_INIT" ] && [ -f "$$IR_INIT" ]; then grep -q "^# (patched-no-ReFL)" "$$IR_INIT" || sed -i "s|^from \\.ReFL import|# (patched-no-ReFL) from .ReFL import|" "$$IR_INIT"; fi'
      - $$REWARD_PY -c "import ImageReward; print('[verify-final] ImageReward', ImageReward.__file__)"
      # ── Preload reward weights & launch reward server on GPU 0 ─────────────
      - export REWARD_BACKENDS_SERVER='hpsv3 imagereward'
      - export STAGE_REWARD_WEIGHTS_LOCAL=1
      - export REWARD_LOCAL_HF_CACHE=/tmp/hf_cache_local
      - REWARD_BACKENDS="$$REWARD_BACKENDS_SERVER" $$REWARD_PY preload_reward_models.py
      - 'if [ -f "$$REWARD_LOCAL_HF_CACHE/.stage_done" ]; then export REWARD_HF_HOME="$$REWARD_LOCAL_HF_CACHE"; else export REWARD_HF_HOME="$$HF_HOME"; fi'
      - export OUT_ROOT=/mnt/data/v-yigu/flux_dts_hpsv3/{run_tag}
      - mkdir -p "$$OUT_ROOT"
      - export REWARD_SERVER_PORT={reward_server_port}
      - export REWARD_SERVER_URL=http://localhost:$$REWARD_SERVER_PORT
      - export REWARD_SERVER_LOG=$$OUT_ROOT/reward_server.log
      - env -u NCCL_P2P_LEVEL -u NCCL_ASYNC_ERROR_HANDLING -u TORCH_NCCL_ASYNC_ERROR_HANDLING CUDA_VISIBLE_DEVICES=0 TOKENIZERS_PARALLELISM=false HF_HOME="$$REWARD_HF_HOME" "$$REWARD_PY" -u reward_server.py --port "$$REWARD_SERVER_PORT" --device cuda:0 --backends $$REWARD_BACKENDS_SERVER --image_reward_model ImageReward-v1.0 > "$$REWARD_SERVER_LOG" 2>&1 &
      - export REWARD_SERVER_PID=$$!
      - trap 'kill $$REWARD_SERVER_PID >/dev/null 2>&1 || true' EXIT
      - export HEALTH_OK=0
      - for i in $$(seq 1 1200); do if curl -s "$$REWARD_SERVER_URL/health" >/dev/null 2>&1; then export HEALTH_OK=1; break; fi; if ! kill -0 "$$REWARD_SERVER_PID" >/dev/null 2>&1; then echo "Reward server died"; tail -n 200 "$$REWARD_SERVER_LOG"; exit 1; fi; sleep 3; done
      - if [ "$$HEALTH_OK" != "1" ]; then echo "Reward server failed to become healthy"; tail -n 400 "$$REWARD_SERVER_LOG"; exit 1; fi
      - curl -s "$$REWARD_SERVER_URL/health" | tee "$$OUT_ROOT/reward_server_health.json"
      - export HF_HUB_OFFLINE=1
      - export TRANSFORMERS_OFFLINE=1
      # ── Run knobs ──────────────────────────────────────────────────────────
      - export PROMPT_FILE=./hpsv2_subset.txt
      - export START_INDEX=0
      - export END_INDEX={num_prompts}
      - export SEED={seed}
      - export STEPS=4
      - export BASELINE_GUIDANCE_SCALE=1.0
      - export N_VARIANTS=1
      - export CFG_SCALES='1.0'
      - export METHODS='{methods}'
      # Search-time reward: hpsv3 (auto-routed via REWARD_SERVER_URL by reward_unified).
      - export REWARD_BACKEND=hpsv3
      - export REWARD_TYPE=hpsv3
      - export REWARD_BACKENDS=hpsv3
      # Phase-1 eval covers BOTH server-hosted rewards.
      - export EVAL_BACKENDS='imagereward hpsv3'
      - export EVAL_BEST_IMAGES=1
      - export EVAL_REWARD_DEVICE=cuda
      - export EVAL_ALLOW_MISSING_BACKENDS=0
      - export USE_QWEN=0
      - export PRECOMPUTE_REWRITES=0
      - export REWARDS_OVERWRITE=0
      - export SAVE_BEST_IMAGES=1
      - export SAVE_IMAGES=0
      - export SAVE_VARIANTS=0
      # ── DTS knobs (mirror the imagereward yaml) ────────────────────────────
      - export DTS_M_ITER={dts_m_iter}
      - export DTS_LAMBDA={dts_lambda}
      - export DTS_PW_C={dts_pw_c}
      - export DTS_PW_ALPHA={dts_pw_alpha}
      - export DTS_C_UCT={dts_c_uct}
      - export DTS_SDE_NOISE_SCALE=0.0
      - export DTS_CFG_BANK=''
      # ── Final transformers/datasets pin (sampling env) ─────────────────────
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "transformers==4.52.4" "tokenizers==0.21.1" "qwen-vl-utils==0.0.14"
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir --no-deps "ftfy>=6.2.3" "regex>=2024.11.6" "xxhash>=3.4.1"
      - /opt/conda/envs/ptca/bin/pip install --no-cache-dir "pandas>=2.1.4" "pyarrow>=14.0.2" "datasets>=2.19.0"
      # ── Restrict sampling to GPUs 1..N-1 (server owns GPU 0) ───────────────
      - export SAMPLING_GPUS=$$((${GPUS} - 1))
      - export CUDA_VISIBLE_DEVICES=$$(seq -s, 1 $$SAMPLING_GPUS)
      - export NUM_GPUS=$$SAMPLING_GPUS
      - 'echo "Sampling on CUDA_VISIBLE_DEVICES=$$CUDA_VISIBLE_DEVICES NUM_GPUS=$$NUM_GPUS REWARD_SERVER_URL=$$REWARD_SERVER_URL"'
      # ── Run (FLUX suite handles dts/dts_star method dispatch) ──────────────
      - bash hpsv2_flux_schnell_ddp_suite.sh
      # ── Phase 2: tear down server, eval hpsv2 + pickscore post-hoc ─────────
      - echo "[posthoc] killing reward server pid=$$REWARD_SERVER_PID"
      - kill "$$REWARD_SERVER_PID" >/dev/null 2>&1 || true
      - for k in $$(seq 1 30); do if ! kill -0 "$$REWARD_SERVER_PID" >/dev/null 2>&1; then break; fi; sleep 1; done
      - fuser -k "$$REWARD_SERVER_PORT/tcp" >/dev/null 2>&1 || true
      - sleep 5
      - trap - EXIT
      - export POSTHOC_EVAL_BACKENDS='hpsv2 pickscore'
      - export POSTHOC_LAYOUT=flux
      - REWARD_PY="$$REWARD_PY" REWARD_SERVER_PORT="$$REWARD_SERVER_PORT" REWARD_HF_HOME="$$REWARD_HF_HOME" PYTHON_BIN="$$PYTHON_BIN" OUT_ROOT="$$OUT_ROOT" POSTHOC_EVAL_BACKENDS="$$POSTHOC_EVAL_BACKENDS" POSTHOC_LAYOUT="$$POSTHOC_LAYOUT" bash post_eval_extra_rewards.sh

    submit_args:
      env:
        DATASET_MOUNT_BLOCK_BASED_CACHE_ENABLED: "true"
        SHARED_MEMORY_PERCENT: "0.5"

  max_trials: 1
  type: grid
  params:
    - name: run_tag
      values: [ "flux_schnell_dts_hpsv3" ]
    - name: hf_token
      values: [ "YOUR_HF_TOKEN_HERE" ]
    - name: num_prompts
      values: [ 8 ]
    - name: seed
      values: [ 42 ]
    - name: methods
      values: [ "baseline dts dts_star" ]
    # Different port from sop_all_models (5102) and fk_steering (5101).
    - name: reward_server_port
      values: [ 5106 ]
    # DTS knobs identical to flux_schnell_dts_imagereward.yaml (M=64 × steps=4 = 256 NFE/prompt).
    - name: dts_m_iter
      values: [ 64 ]
    - name: dts_lambda
      values: [ 1.0 ]
    - name: dts_pw_c
      values: [ 1.0 ]
    - name: dts_pw_alpha
      values: [ 0.5 ]
    - name: dts_c_uct
      values: [ 1.0 ]
