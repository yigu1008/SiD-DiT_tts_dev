# ── Core torch stack ───────────────────────────────────────────────────────────
# Install with: pip install -r requirements_all.txt --index-url https://download.pytorch.org/whl/cu126
# or use the separate --extra-index-url approach; torch/torchvision need the whl index.
torch==2.7.1
torchvision==0.22.1
xformers==0.0.31.post1

# ── Diffusion / HuggingFace ────────────────────────────────────────────────────
accelerate==1.8.1
diffusers==0.33.1
huggingface-hub==0.33.0
tokenizers==0.21.1
transformers==4.52.4
safetensors==0.5.3
peft
sentencepiece==0.2.0

# ── Data / IO ──────────────────────────────────────────────────────────────────
datasets==2.19.0
pyarrow>=14.0.2
pandas>=2.1.4
numpy==1.26.4
blobfile==3.0.0
scipy==1.13.0
pillow==10.3.0
requests==2.31.0

# ── Text / hash utils (pinned — commonly downgraded by reward deps) ────────────
ftfy==6.3.1
regex>=2024.11.6
xxhash>=3.4.1
tqdm==4.66.4
wcwidth==0.2.13

# ── Vision / reward backends ───────────────────────────────────────────────────
timm==1.0.15
open-clip-torch==2.32.0
# image-reward: install AFTER this file, with --no-deps to prevent silent downgrades:
#   pip install --no-deps image-reward==1.5
#   pip install --no-deps git+https://github.com/THUDM/ImageReward.git  (fallback)
# clip (OpenAI): install AFTER open-clip-torch:
#   pip install clip-anytorch
#   pip install git+https://github.com/openai/CLIP.git  (fallback)
# hpsv2x/hpsv3: install with --no-deps to prevent ftfy/regex/xxhash downgrades:
#   pip install --no-deps hpsv2x
#   pip install --no-deps hpsv3
omegaconf>=2.3.0
hydra-core>=1.3.2

# ── Qwen / UnifiedReward ──────────────────────────────────────────────────────
qwen-vl-utils>=0.0.14
openai>=1.40.0

# ── Logging / experiment tracking ─────────────────────────────────────────────
wandb>=0.19,<0.21
protobuf==6.31.1
absl_py
ml_collections

# ── HTTP (required by datasets>=2.19) ─────────────────────────────────────────
httpx>=0.23.0

# ── Cluster overlay extras ────────────────────────────────────────────────────
pyyaml>=6.0.1
click==8.2.1
typing-extensions>=4.11.0
sentry-sdk>=2.0.0
gitpython>=3.1.43
